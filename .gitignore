# ---- macOS / editor noise ----
.DS_Store
*.swp
*.swo
.idea/
.vscode/
*.iml

# ---- Claude Code internal config (per-machine) ----
.claude/
.mcp.json

# ---- Image originals — kept locally only (54 MB of pre-optimization JPGs) ----
assets/images/_originals/

# ---- Approval deck artifacts (huge PDFs, regenerated by build-v1.py) ----
approvals/v1/Taj-Al-Sukn-Approval-Deck-v1.pdf
approvals/v1/final/
approvals/v1/pages/
approvals/v1/feedback/
approvals/pdfs/
approvals/screenshots/
# Keep the build script + feedback template + folder readmes
!approvals/build-v1.py
!approvals/feedback-template.html
!approvals/style.css

# ---- Preview server scratch ----
/tmp/taj-spa-site/
*.log
serve.py.bak

# ---- Generated CSS source kept locally, minified css.css is committed ----
# (Uncomment if you'd rather ship only the minified one.)
# assets/css/style.src.css

# ---- Local environment / secrets ----
# Note: assets/js/supabase-config.js holds ONLY the publishable key,
# which Supabase explicitly designs to be exposed in client code.
# RLS policies protect data — see db/README.md.
.env
.env.local
*.secret
